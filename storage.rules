rules_version = '2';
service firebase.storage {
  match /b/devfest-milano-2025/o {
    // Allow access to all files in the 2025 bucket
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
